# Signal Dashboard — Demo (Sampler)

What it is:

- A lightweight dashboard spec (config + schema + widget templates) for representing signals and confidence.

Design constraints:

- No addiction loops: no notifications, badges, streaks, or gamification.
- Informational display only.

What you get in the paid kit:

- A validation-friendly config schema
- A component library and widget templates
- Snapshot/run structure for deployments
