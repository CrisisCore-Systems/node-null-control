name: Dispatch GH_TOKEN smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: GH CLI version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh --version
